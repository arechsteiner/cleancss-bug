This is just a small example that demonstrates a bug when using gulp-cleancss togeter with gulp-sourcemaps.

After cloning run `npm install` and possibly `npm install gulp -g`. Then run `gulp` to see the bug.
